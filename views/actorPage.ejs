<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="/scripts.js" defer></script>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <img style="max-height: 200px;" src=<%= movies.actorurl %>>
    <div><%= movies.actorname %></div>
    <% if(movies.directormovies.length>0){ %>
        <div>Director of:
            <div style="display: flex;justify-content: flex-start; flex-wrap: wrap;">
                <% for(let i=0;i< movies.directormovies.length; i++){ %>
                    <div style="width: 210px;text-align: center;">
                        <div>
                            <img style="max-height: 200px;" 
                            src=<%= movies.directormovies[i].movie_url %>>
                        </div>
                        <a href="/movies/<%= movies.directormovies[i].movie_id %>">
                            <%= movies.directormovies[i].movie_name %>
                        </a>

                    </div>


                        <% } %>

            </div>


        </div>
    <% } %>
    
    <% if(movies.actormovies.length>0){ %>   
        <div>Starred in: 
            <div style="display: flex;justify-content: flex-start; flex-wrap: wrap;">
                <% for(let i=0;i< movies.actormovies.length; i++){ %>
                    <div style="width: 210px;text-align: center;">
                        <div>
                            <img style="max-height: 200px;" src=<%=movies.actormovies[i].movie_url %>>
                        </div>
                        <a href="/movies/<%= movies.actormovies[i].movie_id %>">
                            <%= movies.actormovies[i].movie_name %>
                        </a>
                    </div>
                <% } %>
                    

            </div>

 
        </div>
    <% } %>
    <div>
        <button type="button" onclick="displayPersonForm()">Edit person</button>

        <button onclick="window.location.href='/actors/<%= movies.id %>/delete/'">Delete person</button>
    </div>   
    <%- include("editPersonForm",{movies:movies,allMovies:allMovies}) %>

    
    
    
</body>
</html>